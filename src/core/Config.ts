/**
 * The number of pulses (ticks) per quarter note. This defines the internal resolution for all time values.
 */
export const PPQN = 480;

/**
 * The number of samples per buffer.
 */
export const BUFFER_SIZE = 128;

/**
 * The sample rate of the audio context.
 */
export const SAMPLE_RATE = 44100;

/**
 * The minimum volume in dB.
 */
export const MIN_VOLUME_DB = -60;

/**
 * The maximum volume in dB.
 */
export const MAX_VOLUME_DB = 6;
